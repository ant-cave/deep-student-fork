-- ============================================================================
-- V20260206: 修复缺失的 vfs_index_segments 索引
-- ============================================================================
--
-- 目的：补齐旧库缺失的 idx_vfs_index_segments_unit 索引，避免迁移验证失败
-- 安全：使用 IF NOT EXISTS，幂等可重复执行
-- ============================================================================

CREATE INDEX IF NOT EXISTS idx_vfs_index_segments_unit ON vfs_index_segments(unit_id);
